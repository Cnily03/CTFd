version: "3"
services:
    web:
        build: .
        volumes:
            - "$PWD/flag:/flag:ro"
        environment:
            TZ: Asia/Shanghai
            FLAG: ${FLAG}
        restart: always
        ports:
          - 9999:80
        networks:
            default:
            # public:

networks:
    default:
    # public:
    #   external:
    #      name: frp_containers